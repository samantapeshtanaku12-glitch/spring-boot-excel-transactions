# spring-boot-excel-transactions
A full-stack Java Spring Boot application that imports Excel files, parses transaction data with Apache POI, persists the data into PostgreSQL via Spring Data JPA, exposes REST APIs, and displays uploaded transactions in a responsive web interface.
